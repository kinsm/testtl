import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        marginLeft: 16,
        height: 32,
        justifyContent: 'center',
        borderBottomWidth: 1,
    },
    text: {
        fontSize: 16,
    },
});
